import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class settings here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class settings extends Button
{
    /**
     * Act - do whatever the settings wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public settings(World world, Color color, int x, int y, String text, int dimensions, int fontSize){
        super(world,color,x,y,text,dimensions,fontSize);
    }
    
    public void act() 
    {
        // Add your action code here.
    }    
}
